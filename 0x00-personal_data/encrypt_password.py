#!/usr/bin/env python3
""""
hash_password function that expects one string argument name password and returns a salted, hashed passwor
"""
import bcrypt


def hash_password(password: str) -> bytes:
    return hashed_password
